<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/studentLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:layoutDirection="rtl">

    <!-- شريط علوي يحتوي على رسالة الترحيب وزر تسجيل الخروج -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="16dp">

        <TextView
            android:id="@+id/welcomeText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="مرحباً، الطالب"
            android:textDirection="rtl"
            android:textSize="16sp"
            android:textStyle="bold|italic" />

        <Button
            android:id="@+id/logoutButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="تسجيل الخروج"
            app:rippleColor="#4048BD"
            android:backgroundTint="#0D47A1" />
    </LinearLayout>

    <!-- قائمة النقاط -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/moduleList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingBottom="8dp" />

    <!-- زر حساب المعدل -->
    <Button
        android:id="@+id/calculateButton4"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📊 احسب المعدل"
        android:textSize="18sp"
        android:layout_marginTop="16dp"
        android:backgroundTint="#3F51B5"
        android:textColor="@android:color/white" />

    <!-- عرض النتيجة -->
    <TextView
        android:id="@+id/tvResult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="16dp"
        android:text="النتيجة ستظهر هنا"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#333333" />
</LinearLayout>